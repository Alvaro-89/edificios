<p id="notice"><%= notice %></p>

<div class="card">
  <div class="card-body">
    <h1>Edificios 🏦</h1>
    <table class='table table-hover'>
      <thead>
        <tr>
          <th>ID</th>
          <th>Nombre</th>
          <th>Dirección</th>
          <th>Ciudad</th>
          <th colspan="3"></th>
        </tr>
      </thead>

      <tbody>
        <% @buildings.each do |building| %>
          <tr>
            <td><%= building.id %></td>
            <td><%= link_to building.name, building_path(building) %></td>
            <td><%= building.address %></td>
            <td><%= building.city %></td>
            <td><%= link_to 'Ver edificio', building, class:'btn btn-outline-info text-info' %></td>
            <td><%= link_to 'Editar', edit_building_path(building), class: "btn btn-outline-warning text-warning" %></td>
            <td><%= link_to 'Eliminar', building, class:'btn btn-outline-danger text-danger', method: :delete, data: { confirm: '¿Estás seguro que quieres eliminar este edificio?' } %></td>
          </tr>
        <% end %>
      </tbody>
    </table>

    <br>
    <div class="mb-3 text-end">
      <%= link_to 'Nuevo edificio', new_building_path, class: 'btn btn-primary text-white' %>
    </div>
  </div>
</div>