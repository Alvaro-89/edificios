<p id="notice"><%= notice %></p>

<div class="card">
  <div class="card-body">
    <h1>Departamentos 🚪</h1>
    <table class='table table-hover'>
      <thead>
        <tr>
          <th class="text-center">Número de departamento</th>
          <th>Edificio</th>
          <th colspan="3"></th>
        </tr>
      </thead>

      <tbody>
        <% @apartments.each do |apartment| %>
          <tr>
            <td class="text-center"><%= apartment.apartment_number %></td>
            <td class="text-left"><%= link_to apartment.building.name, building_path(apartment.building) %></td>
            <td><%= link_to 'Ver departamento', apartment, class:'btn btn-outline-info text-info' %></td>
            <td><%= link_to 'Editar', edit_apartment_path(apartment), class: "btn btn-outline-warning text-warning" %></td>
            <td><%= link_to 'Eliminar', apartment, class:'btn btn-outline-danger text-danger', method: :delete, data: { confirm: 'Are you sure?' } %></td>
          </tr>
        <% end %>
      </tbody>
    </table>

    <br>
    <div class="mb-3 text-end">
        <%= link_to 'Nuevo departamento', new_apartment_path, class: 'btn btn-primary text-white' %>
    </div>
  </div>
</div>